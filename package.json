{
  "name": "weshare",
  "version": "1.0.0",
  "private": true,
  "description": "WeShare \u2014 static UI + Vercel serverless API (PayFast)",
  "scripts": {
    "dev": "vercel dev",
    "start": "npx http-server -p 3000 ."
  },
  "engines": {
    "node": ">=18"
  }
}